{
  "name": "@gh-automation/shared-types",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "biome lint src/",
    "format": "biome format --write src/",
    "check": "biome check --write src/"
  },
  "devDependencies": {
    "@gh-automation/typescript-config": "workspace:*",
    "typescript": "^5.3.3"
  }
}
